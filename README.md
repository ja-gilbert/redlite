# redlite

A Redis-compatible server written in Python. Implements the RESP wire
protocol and a subset of the Redis command set over a TCP server, so
standard Redis clients can talk to it unmodified.

## Status

In progress.

## Running
python -m redlite
